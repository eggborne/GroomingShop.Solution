@{
  Layout = "_Layout";
}

@using GroomingShop.Models;

<h2>All Appointments</h2>
<hr />

@if (@Model.Count == 0)
{
  <h3>No appointments have been added yet!</h3>
} 

<ul class="appointment-list">
  @foreach(Appointment appointment in Model)
  {
    <li>
      <div>@appointment.Pet.Name | @appointment.Pet.Breed</div>
      <div>@appointment.Parent.FirstName @appointment.Parent.LastName</div>
      <div>@appointment.Groomer.Role: @appointment.Groomer.FirstName @appointment.Groomer.LastName |@DateOnly.FromDateTime(appointment.Date) @TimeOnly.FromDateTime(appointment.StartTime) to @TimeOnly.FromDateTime(appointment.EndTime)</div>
      <a href='@Url.Action("Edit", new { id = appointment.AppointmentId })'>
        <button class="yellow">Edit</button>
      </a>
    </li>
  }
</ul>

@* <a href='@Url.Action("Create")'>
  <button class="green">Add New Groomer</button>
</a>
<a href='@Url.Action("Create", "Patients")'>
  <button class="green">Add New Patient</button>
</a> *@
<a href='@Url.Action("Index", "Home")'>
  <button>Home</button>
</a>